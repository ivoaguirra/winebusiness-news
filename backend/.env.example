# WineBusiness.news - Strapi Backend Environment Variables

# ===========================================
# SERVER
# ===========================================
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# ===========================================
# DATABASE
# ===========================================
# Option 1: Use DATABASE_URL (recommended for cloud deployments)
# Format: postgresql://user:password@host:port/database?sslmode=require
# Uncomment the line below and comment out the individual credentials
# DATABASE_URL=postgresql://winebusiness:your_password@localhost:5432/winebusiness

# Option 2: Use individual credentials (for local development)
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=winebusiness
DATABASE_USERNAME=winebusiness
DATABASE_PASSWORD=your_password_here
DATABASE_SSL=false

# ===========================================
# SECRETS
# ===========================================
# Generate secure keys with: openssl rand -base64 32
# IMPORTANT: Keep these secret and unique per environment

# App Keys (comma-separated, minimum 4 keys)
APP_KEYS=key1,key2,key3,key4

# Security Salts and Secrets
API_TOKEN_SALT=your_api_token_salt
ADMIN_JWT_SECRET=your_admin_jwt_secret
TRANSFER_TOKEN_SALT=your_transfer_token_salt
JWT_SECRET=your_jwt_secret

# ===========================================
# OPTIONAL: EXTERNAL SERVICES
# ===========================================
# AWS S3 for uploads (uncomment to enable)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_BUCKET=winebusiness-uploads

# SMTP for emails (uncomment to enable)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@winebusiness.news
# SMTP_PASSWORD=your_smtp_password
