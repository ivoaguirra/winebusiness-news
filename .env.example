# WineBusiness.news - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# GENERAL
# ===========================================
NODE_ENV=production
PORT=1337

# ===========================================
# DATABASE (PostgreSQL)
# ===========================================
# Option 1: Use DATABASE_URL (recommended for cloud deployments)
# Format: postgresql://user:password@host:port/database?sslmode=require
# DATABASE_URL=postgresql://winebusiness:your_password@localhost:5432/winebusiness

# Option 2: Use individual credentials (for Docker Compose and local development)
POSTGRES_DB=winebusiness
POSTGRES_USER=winebusiness
POSTGRES_PASSWORD=your_secure_password_here

# Database SSL (set to true for cloud databases like Railway, Heroku, AWS RDS)
DATABASE_SSL=false

# ===========================================
# STRAPI CMS - SECRETS
# ===========================================
# Generate secure keys with: openssl rand -base64 32
# IMPORTANT: Keep these secret and unique per environment

# App Keys (comma-separated, minimum 4 keys)
APP_KEYS=key1,key2,key3,key4

# Security Salts and Secrets
API_TOKEN_SALT=your_api_token_salt_here
ADMIN_JWT_SECRET=your_admin_jwt_secret_here
TRANSFER_TOKEN_SALT=your_transfer_token_salt_here
JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_encryption_key_here

# ===========================================
# STRAPI CMS - API ACCESS
# ===========================================
# API Token for frontend to access Strapi (create in Strapi Admin Panel)
STRAPI_API_TOKEN=your_strapi_api_token

# ===========================================
# FRONTEND (Next.js)
# ===========================================
# Public URL of the Strapi API (as seen by the browser)
NEXT_PUBLIC_STRAPI_URL=https://winebusiness.news

# Public URL of the site
NEXT_PUBLIC_SITE_URL=https://winebusiness.news

# ===========================================
# ANALYTICS & MONETIZATION
# ===========================================
# Google Analytics 4 Measurement ID
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Google Ad Manager Network Code
NEXT_PUBLIC_GAM_NETWORK_CODE=12345678

# ===========================================
# NEWSLETTER
# ===========================================
# Webhook URL for newsletter integrations (Mailchimp, Brevo, etc.)
NEWSLETTER_WEBHOOK_URL=https://your-webhook-url.com

# ===========================================
# OPTIONAL: EXTERNAL SERVICES
# ===========================================
# AWS S3 for media storage (optional - uncomment to enable)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_BUCKET=winebusiness-uploads

# SMTP for transactional emails (optional - uncomment to enable)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@winebusiness.news
# SMTP_PASSWORD=your_smtp_password
# SMTP_FROM=noreply@winebusiness.news
